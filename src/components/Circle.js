

const Circle = ({ bottom, right, height, width }) => {
    return (
        <div className='circle1' style={{bottom: bottom, right: right, height: height, width: width}}>
            
        </div>
    )
}

export default Circle
